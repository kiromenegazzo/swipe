import React from "react";

const Settings = ({ className }) => {
  return (
    <div className={className}>
      <button>Remove</button>
      <button>Edit</button>
      <button>Save</button>
    </div>
  );
};

export { Settings };
